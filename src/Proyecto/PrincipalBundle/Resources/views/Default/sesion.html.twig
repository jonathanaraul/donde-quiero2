
  	<div id="registro">
  		{%if datos.usuario is null %}
      <p><a href="{{path('proyecto_principal_acceso')}}">Iniciar Sesión</a> / <a href="{{path('proyecto_perfil_crearcuenta')}}">Registrarse</a> </p>
      {%else%}
	  <p>
	  	<a href="{{path('proyecto_perfil_privado')}}">{{datos.usuario}}</a> - 
        {%if datos.rol%}
	  	<a href="{{path('proyecto_principal_gestion')}}">Administrador</a> - 
        {%endif%}
	  	<a href="{{path('proyecto_principal_cerrarsesion')}}">Cerrar Sesión</a>
    </p>
      {%endif%}


   </div>
	<div class="clearfix"></div> 
    <!-- Text Section End -->