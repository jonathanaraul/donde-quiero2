		{%if dataPaginacion.paginacion == false%}
		<a href="{{ path('project_front_espacio_grupal') }}">{{dataPaginacion.obtenidos}} de {{dataPaginacion.total}} (Ver más)</a>
		{%else%}
		

        {% for i in range(1, dataPaginacion.numPaginacion) %}
         <span style="cursor:pointer;color: #FFF;" class="numero-paginacion-verde" indice="{{ i - 1}}" numResults="{{dataPaginacion.numResults }}">{{ i }}</span>&nbsp; 
        {% endfor %}
		<span style="color: #000;" class=""> {{dataPaginacion.total}} Resultados</span>
		{%endif%}